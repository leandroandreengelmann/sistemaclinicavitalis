/** @type {import('next').NextConfig} */
const nextConfig = {
  // Adicione configurações específicas do Next.js aqui se necessário
  // Exemplo: reactStrictMode: true,
};

export default nextConfig; 